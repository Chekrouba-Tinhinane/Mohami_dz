version: '3'

services:
  backend:
    build:
      context: .
      dockerfile: api_project/Dockerfile
    ports:
      - "8000:8000"

  frontend:
    build:
      context: .
      dockerfile: client/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
